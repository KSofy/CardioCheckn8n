// Root build.gradle (Groovy DSL)
plugins {
    id 'com.android.application' version '8.12.0' apply false // Asegúrate que esta versión coincida con la tuya
}

task clean(type: Delete) {
    delete rootProject.buildDir
}